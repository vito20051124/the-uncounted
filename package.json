{
  "name": "the-uncounted",
  "private": true,
  "version": "0.0.0-p0",
  "type": "module",
  "description": "The Uncounted / 無籍者 — a survival sandbox set in Serein. P0 loop-validation prototype.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/build-data.mjs && vite",
    "build": "node scripts/build-data.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-data.mjs",
    "smoke": "node scripts/smoke.ts",
    "pareto": "node scripts/pareto-check.ts",
    "check": "node scripts/build-data.mjs --check && node scripts/cond-sat-test.mjs && node scripts/validate-data.mjs && tsc --noEmit && node scripts/pareto-check.ts && node scripts/unit-test.ts && node scripts/reach-test.ts && node scripts/smoke.ts && node scripts/save-test.ts && node scripts/balance.ts 200",
    "balance": "node scripts/balance.ts 300",
    "save-test": "node scripts/save-test.ts",
    "unit-test": "node scripts/unit-test.ts",
    "build:data": "node scripts/build-data.mjs",
    "reach-test": "node scripts/reach-test.ts",
    "cond-sat-test": "node scripts/cond-sat-test.mjs"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "yaml": "^2.9.0"
  },
  "license": "AGPL-3.0-or-later"
}
